// SDK.odin
package core

// all engine functionallity aside from init(), run(), and destroy() should be @(private)
// SDK is responsible for the public API, and will serve as a wrapper for core modules.

